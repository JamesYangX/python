# '=' 연산자는 실제로 리스트를 복사하지 않음
# 리스트를 복사하려면 슬라이싱을 사용하거나 copy.copy() 함수를 사용해야 함
a = [1, 2, 3] 
b = a
c = a[:]
d = copy.copy(a)
a[0] = 4
print(a, b, c, d)

# 결과
[4, 2, 3] [4, 2, 3] [1, 2, 3] [1, 2, 3]

# 리스트 자체를 복사하려면 [:]로 복사, 혹은 copy.copy()
# b = a 로 할당하는 경우 b도 a가 바뀌면 같이 바뀜
